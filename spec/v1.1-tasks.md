# YT Restore v1.1 Tasks

## 1. Settings Surface

- [x] Add a popup-based settings surface for the extension action.
- [x] Add controls for reversing overlay button order and hiding overlays when native buttons are present.
- [x] Add a rebuild action to refresh overlays from scratch.
- [x] Add a Donate link that opens the KoFi page in a new tab.

## 2. Shared Settings

- [x] Add persistent extension settings with safe defaults.
- [x] Load settings in the content script and update overlays when they change.

## 3. Overlay Behavior

- [x] Reverse the overlay button order when the setting is enabled.
- [x] Apply the same ordering behavior to player-page buttons.
- [x] Detect native YouTube controls and skip custom overlays when they are present.

## 4. Rebuild Flow

- [x] Add a reset message path from the popup to the content script.
- [x] Clear extension-owned transient state, DOM markers, and page-storage leftovers during rebuild.
- [x] Re-run discovery immediately after reset so overlays are rebuilt in place.

## 5. Validation

- [ ] Verify settings persist across page reloads.
- [ ] Verify reverse order affects both thumbnail and player buttons.
- [ ] Verify native-button suppression prevents duplicate overlays.
- [ ] Verify rebuild removes and recreates overlays without a full browser reload.
- [ ] Verify the Donate link points to the KoFi page and opens in a new tab.